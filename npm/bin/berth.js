#!/usr/bin/env node
// berth.js - npm bin entry point that delegates to the Go binary.
// To be implemented in a later phase.
console.error("berth: not yet installed. Run from Go binary directly.");
process.exit(1);
